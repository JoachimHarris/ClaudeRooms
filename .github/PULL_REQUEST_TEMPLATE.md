## What changed

## Why

## Testing

- [ ] `pnpm test` passes
- [ ] `pnpm lint` / `pnpm typecheck` / `pnpm build` pass
- New/changed behavior is covered by tests

## Security impact

Does this touch authentication, authorization, the protocol, rendering of
room content, logging, or anything filesystem-adjacent? If yes, explain and
update `docs/security/threat-model.md` if needed. If no, say "none".

## Screenshots (UI changes)
